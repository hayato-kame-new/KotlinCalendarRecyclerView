<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="to.msn.wings.kotlincalendarrecyclerview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinCalendarRecyclerView">
        <activity
            android:name=".TimeScheduleActivity"
            android:exported="false"
            android:label="@string/title_activity_time_schedule"
            android:theme="@style/Theme.KotlinCalendarRecyclerView.NoActionBar" />
        <activity
            android:name=".ScheduleFormActivity"
            android:exported="false" />
        <activity
            android:name=".MonthCalendarActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>